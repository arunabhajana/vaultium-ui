import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // reactCompiler: true, // Disabled: Causes fatal Turbopack compiler panics in Next 15 with complex SVGs/components
};

export default nextConfig;
